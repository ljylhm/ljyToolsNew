// 面试常用js代码搜集
export const DataStructure = {
    bubleSort(data = []) {
        let len = data.length
        let public = null
        for (let i = 0; i < len; i++) {
            for (let j = i; j < len; j++) {
                public = data[j]
            }
        }
    }
}

